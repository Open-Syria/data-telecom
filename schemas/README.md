# Schemas

This directory contains draft 2020-12 JSON Schemas for the canonical telecom data files,
source-import manifests, and release manifests.

Run `pnpm run validate:schemas` to compile the schemas and validate the canonical data,
fixtures, examples, and import manifests.
